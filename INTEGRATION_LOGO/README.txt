INTEGRATION — LOGO 07 FINAL ASSET PACKAGE

SOURCE OF TRUTH
The supplied Logo #07 artwork is preserved as the visual source of truth.

CONTENTS
SVG/ — 4 master files:
  full_lockup_dark.svg
  full_lockup_light.svg
  icon_only_dark.svg
  icon_only_light.svg

PNG/full/ — full lockup exports at 512, 1024, 2048 px, dark + light.
PNG/icon/ — icon exports from 16 px through 1024 px, plus 768/1536 working sizes,
             dark + light.
favicon/ — favicon.ico containing 16/32/48/64 px.
PDF/ — dark and light print-ready PDFs.

IMPORTANT VECTOR NOTE
No original vector artwork was supplied with the reference image. Therefore the SVG
masters in this package preserve the exact supplied artwork by embedding the source
image; they are SVG containers, not reconstructed vector paths. This avoids changing
the distinctive shape of the Logo #07 mark. For a genuinely path-based SVG, the
original vector/logo source (Illustrator, Figma, EPS, SVG, or similar) is required.
