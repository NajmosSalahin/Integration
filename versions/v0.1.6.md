# Version 0.1.6

**Date:** 2026-09-14
**Status:** Pre-Release — v0.1.6 In Preparation

---

## Features Shipped

| Feature | Description |
|---|---|
| Men's Shirts category | 4 new navy long-sleeve shirts (Classic Poplin, Denim Weave, Flannel Edition, Oxford Button-Down) at 750 BDT tagged `mens-shirts` |
| IU catalog expansion | 12 new Islamic University t-shirt designs (TSCC, The Central Mosque, Red Double-Decker, Crossing in Color, and 8 more) |
| New Cultural T-Shirt category | 4 t-shirts (`cultural-tshirts`) — Anonto Kaler Gaan, Bengal Stories, Fisherman's Silence, Melancholy Golden Highway |
| New Landscape T-Shirt category | 8 t-shirts (`landscape-tshirts`) — Clay Vessels, Cooling Towers at Dusk, Ember Sky Lone Wing, Shikara Sunbeams, Skyward Cross, Sundown Through the Wires, The Green Tunnel, The Hollow Tower |
| New Artisan T-Shirt category | 4 t-shirts (`artisan-tshirts`) — Lanterns to Nowhere, Liquid Gold Tide, Murobbi Murobbi, River Bend City From Above |
| Literature catalog expansion | 2 new book-cover designs — Aranyak (2), Megh Bolechhe Jabo Jabo |
| Featured dashboard populated | 3 new IU designs flagged `featured` so the homepage carousel has content |

---

## API Endpoints

| Method | Route | Auth | Purpose |
|---|---|---|---|
| — | — | — | No API changes in this version |

---

## Data Model Changes

```
Product: {
  featured: Boolean — flag already existed in schema but was never set by the seeder
}
```

**Added:**
- No schema fields added; new products use existing schema with new `tags` values.

**Modified:**
- `featured` — the seeder previously never copied the `featured` flag from seed arrays into product documents, so every product defaulted to `false`. Now `buildProducts()` propagates `featured: !!seedRecord.featured`.

**Removed:**
- Nothing removed.

---

## Dependencies

**Added:** none

**Removed:** none

**Updated:** none

---

## Bug Fixes

| Issue | Fix |
|---|---|
| Homepage featured carousel always empty | `seed.js` `buildProducts()` never wrote the `featured` flag — added `featured: !!tee.featured` / `!!item.featured` to the IU, aesthetic, and literature build loops. Re-seeded with 10 featured products. |
| Shop build error `Expected ")" but found "interpret"` | Stray word `interpret` removed from `Shop.jsx` slice call. |
| Shop pagination syntax error | Literal `\n` escapes were baked into `Shop.jsx` line 109; expanded into real multi-line statements and removed the unused `window` shadowing. |

---

## Breaking Changes

| Change | Migration |
|---|---|
| Database re-seeded | Database is wiped and re-seeded with 155 products (was 121). No customer data involved since products only. |
| Three new categories added | `cultural-tshirts`, `landscape-tshirts`, `artisan-tshirts` added to the shop sidebar and homepage category grid; requires re-seed to populate. |

---

## Future Upgrades

| Priority | Upgrade | Notes |
|---|---|---|
| Medium | Add Womens_Tshirt / Womens_Shirt / bags categories | Image folders exist in `Product Images/` but are empty; seed supporting arrays not yet added |
| Low | Chunk-split client bundle | Vite warns bundle > 500 kB; consider dynamic imports / manualChunks |

---

## Deploy Notes

- No environment variable changes
- Re-run `npm run seed` (server) on deploy environments to pick up the 19 new products
- `uploaded_urls.json` grew from 121 → 155 keys (19 new Cloudinary image sets)